get:
  summary: Returns a list of history sets.
  description: Returns a list of history sets.
  parameters:
    # - $ref: "../components/parameters.yaml#/components/parameters/LimitParam"
    # - $ref: "../components/parameters.yaml#/components/parameters/OffsetParam"
  # responses:
  #   "200":
  #     description: A JSON array of history sets
  #     content:
  #       application/json:

post:
  summary: Create a new history set.
  description: Create a new history set.
  requestBody:
    required: true
    content:
      application/json:
        schema:

  # responses:
  #   "200": # status code
  #     description: A JSON array of user names
  #     content:
  #       application/json:
  #         schema:
